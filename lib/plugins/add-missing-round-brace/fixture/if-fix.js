if (parentPath.isLabeledStatement(1)) {
}

if (isLabeledStatement(1)) {
}

 if (name === 'package.json')
     return PACKAGE;

if (isNode(files)) {
    const {node} = createPath({
        dir: filename,
        type,
        filesystem,
        isStop,
    });

    return node;
}
