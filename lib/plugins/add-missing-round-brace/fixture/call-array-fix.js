const ts = tseslint.config([]);
t.calledWith(save, ['/README.md', event, 'hello']);

function convertPaddingLines() {
}
test('typos: save', async (t) => {
    t.calledWith(save, ['/README.md', event, 'hello']);
    t.end();
});
