const a = {
    a: b,
    isOneOfKeywords(token, keywords) {
    }
}

export class DrawCircle {
    #init() {
        addStyle(this.#canvas, {
            height: `${height}px`
        });
    }

    async draw() {
    }
}
