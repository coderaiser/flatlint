export const fromSimple = (simple) => {
    for (const line of list)) {
    }
}

function createDirectory(filename) {
    return {
        filename,
        type: 'directory',
        files: [],
    }
}